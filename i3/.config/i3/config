# i3 config file (v4)
# Generated by i3-config-wizard(1)
# Please see https://i3wm.org/docs/userguide.html for a complete reference!

# Mod Key
set $mod Mod4

# Execute Always

exec_always --no-startup-id sudo /home/yosefsaaid/.cargo/bin/kanata -c /home/yosefsaaid/.config/kanata/config.kbd
exec_always --no-startup-id picom --experimental-backends 
exec_always --no-startup-id /home/yosefsaaid/local/bin/autotiling
exec_always --no-startup-id killall polybar 
exec_always --no-startup-id ~/.config/polybar/launch_polybar.sh
exec_always feh --bg-scale ~/Pictures/Wallpaper/_1.jpg
exec --no-startup-id dex --autostart --environment i3
exec --no-startup-id xss-lock --transfer-sleep-lock -- i3lock-fancy --nofork
exec --no-startup-id nm-applet
exec wezterm
exec --no-startup-id copyq


# Font
font pango:monospace   SF Pro Rounded: 14

# Color Scheme (Tokyo Night)
# Base colors
set $rosewater #f7768e
set $flamingo  #ff9e64
set $pink      #ff007c
set $mauve     #bb9af7
set $red       #f7768e
set $maroon    #ff9e64
set $peach     #ff9e64
set $yellow    #e0af68
set $green     #9ece6a
set $teal      #73daca
set $sky       #b4f9f8
set $sapphire  #7dcfff
set $blue      #7aa2f7
set $lavender  #bb9af7
set $text      #a9b1d6
set $subtext1  #9aa5ce
set $subtext0  #cfc9c2
set $overlay2  #565f89
set $overlay1  #414868
set $overlay0  #3b4261
set $surface2  #292e42
set $surface1  #24283b
set $surface0  #1f2335
set $base      #1a1b26
set $mantle    #16161e
set $crust     #13131a

# Window Colors
client.focused           $mauve    $base       $text   $blue      $mauve
client.focused_inactive  $overlay0 $base       $text   $blue      $overlay0
client.unfocused         $overlay0 $base       $text   $blue      $overlay0
client.urgent            $peach    $base       $peach  $overlay0  $peach
client.placeholder       $overlay0 $base       $text   $overlay0  $overlay0
client.background        $base

# Border and Gaps
for_window [class=".*"] border pixel 1
gaps inner 4
gaps outer 10

# Volume Control Bindings
set $refresh_i3status killall -SIGUSR1 i3status

# Workspace Configuration
set $ws1 "1 "
set $ws2 "2 "
set $ws3 "3 "
set $ws4 "4 "
set $ws5 "5 󰽉"
set $ws6 "6"
set $ws7 "7"
set $ws8 "8 󰊻"
set $ws9 "9 "
set $ws10 "10 "

# Workspace Bindings
bindsym $mod+1 workspace number $ws1
bindsym $mod+2 workspace number $ws2
bindsym $mod+3 workspace number $ws3
bindsym $mod+4 workspace number $ws4
bindsym $mod+5 workspace number $ws5
bindsym $mod+6 workspace number $ws6
bindsym $mod+7 workspace number $ws7
bindsym $mod+8 workspace number $ws8
bindsym $mod+9 workspace number $ws9
bindsym $mod+0 workspace number $ws10

# Move Focused Container to Workspace
bindsym $mod+Shift+1 move container to workspace number $ws1; workspace number $ws1
bindsym $mod+Shift+2 move container to workspace number $ws2; workspace number $ws2
bindsym $mod+Shift+3 move container to workspace number $ws3; workspace number $ws3
bindsym $mod+Shift+4 move container to workspace number $ws4; workspace number $ws4
bindsym $mod+Shift+5 move container to workspace number $ws5; workspace number $ws5
bindsym $mod+Shift+6 move container to workspace number $ws6; workspace number $ws6
bindsym $mod+Shift+7 move container to workspace number $ws7; workspace number $ws7
bindsym $mod+Shift+8 move container to workspace number $ws8; workspace number $ws8
bindsym $mod+Shift+9 move container to workspace number $ws9; workspace number $ws9
bindsym $mod+Shift+0 move container to workspace number $ws10; workspace number $ws10
# Window Management
floating_modifier $mod

# Focus Bindings
bindsym $mod+h focus left
bindsym $mod+j focus down
bindsym $mod+k focus up
bindsym $mod+l focus right

# Move Window Bindings
bindsym $mod+Shift+h move left
bindsym $mod+Shift+j move down
bindsym $mod+Shift+k move up
bindsym $mod+Shift+l move right


# Resize Mode
mode "resize" {
    bindsym h resize shrink width 10 px or 10 ppt
    bindsym j resize grow height 10 px or 10 ppt
    bindsym k resize shrink height 10 px or 10 ppt
    bindsym l resize grow width 10 px or 10 ppt
    bindsym Return mode "default"
    bindsym Escape mode "default"
    bindsym $mod+r mode "default"
}
bindsym $mod+r mode "resize"

# Screenshots
bindsym $mod+c exec flameshot gui
bindsym $mod+s exec flameshot full

# Other Bindings
bindsym $mod+x exec i3lock-fancy  
# bindsym $mod+b exec flatpak run io.github.zen_browser.zen
# Volume control
bindsym $mod+u exec pactl set-sink-volume @DEFAULT_SINK@ +5%
bindsym $mod+m exec pactl set-sink-volume @DEFAULT_SINK@ -5%
bindsym $mod+t exec pactl set-sink-mute @DEFAULT_SINK@ toggle

# Media control (playerctl should still work with PipeWire)
bindsym $mod+n exec playerctl next
bindsym $mod+p exec playerctl previous
bindsym $mod+Shift+p exec playerctl play
bindsym $mod+Shift+b exec playerctl pause

bindsym $mod+Return exec wezterm
bindsym $mod+Shift+f exec thunar 
bindsym $mod+q kill
bindsym $mod+d exec --no-startup-id dmenu_run
bindsym $mod+space exec rofi -show combi
# Layout and Tiling
# bindsym $mod+v split v
bindsym $mod+f fullscreen toggle
# bindsym $mod+s layout stacking
bindsym $mod+w layout tabbed
bindsym $mod+e layout toggle split
bindsym $mod+Shift+space floating toggle
bindsym $mod+a focus parent
# Reload/Restart/Exit
bindsym $mod+Shift+c reload
bindsym $mod+Shift+r restart
bindsym $mod+Shift+e exec "i3-nagbar -t warning -m 'You pressed the exit shortcut. Do you really want to exit i3? This will end your X session.' -B 'Yes, exit i3' 'i3-msg exit'"


bindsym $mod+v exec --no-startup-id copyq show
bindsym $mod+shift+v exec --no-startup-id copyq hide

for_window [instance="copyq"] floating enable
for_window [instance="copyq"] resize set 1000 600
for_window [instance="copyq"] move position center
for_window [instance="copyq"] border pixel 5

bindsym $mod+Tab workspace back_and_forth

